<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only: permits cleartext HTTP to the emulator's host-loopback alias
  (10.0.2.2), matching the API's default local port. Never merged into
  release builds — release has no network security config override and
  keeps the platform's HTTPS-only default.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
