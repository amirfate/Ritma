# Ritma API environment. Copy to `.env` for local development:
#   cp apps/api/.env.example apps/api/.env

# Runtime environment: development | test | production
NODE_ENV=development

# HTTP port the Nest application listens on.
PORT=3000
