# Ritma Dashboard environment. Copy to `.env.local` for local development:
#   cp apps/dashboard/.env.example apps/dashboard/.env.local

# Base URL of the Ritma API. Server-only — read only by Route Handlers,
# middleware, and Server Components, never bundled into client JS.
RITMA_API_BASE_URL=http://localhost:3000
